[project]
name = "test-code-guard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "'ahmed-mohamed6'", email = "'am7830468@gmail.com'" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
test-code-guard = "test_code_guard:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
